<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.studyEnglish.mapper.MemoryRecordMapper">

    <select id="findReviewWordCount" resultType="java.lang.Integer">
        select COUNT(*) from memory_record
        where user_id = #{userId} and CURRENT_DATE >= nextmemory_time and phase &lt; 6
    </select>
    <select id="findReviewWordId" resultType="java.lang.Integer">
        select word_id from memory_record
        where user_id = #{userId} and CURRENT_DATE() >= nextmemory_time and phase &lt; 6
    </select>
</mapper>
